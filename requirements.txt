# TikTok Monitor — Python 依赖
# 版本号与本机环境一致（2026-03-27 校对）

# Web 框架
flask==3.1.3
flask-socketio==5.6.1
flask-login==0.6.3

# TikTok 直播
TikTokLive==6.6.5

# HTTP 客户端（支持 SOCKS5 代理）
httpx[socks]==0.28.1
requests==2.32.5

# 语音识别（Whisper）
openai-whisper==20250625

# PyTorch（CPU 基础版；NVIDIA GPU 环境下替换为 CUDA 版本）
# CUDA 版安装命令：
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0

# Excel 报告
openpyxl==3.1.5

# 语言检测
langdetect==1.0.9
langid==1.1.6

# 时区处理
pytz==2025.2

# 事件循环（SocketIO 需要）
eventlet==0.35.2

# 工具库
python-dotenv==1.0.1
